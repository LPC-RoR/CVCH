<%= form_with(model: propuesta, local: true) do |form| %>
  <% if propuesta.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(propuesta.errors.count, "error") %> prohibited this propuesta from being saved:</h2>

      <ul>
      <% propuesta.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :instancia_id %>
    <%= form.number_field :instancia_id %>
  </div>

  <div class="field">
    <%= form.label :publicacion_id %>
    <%= form.number_field :publicacion_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
