<p id="notice"><%= notice %></p>

<h1>Filo Esp Esps</h1>

<table>
  <thead>
    <tr>
      <th>Parent</th>
      <th>Child</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @filo_esp_esps.each do |filo_esp_esp| %>
      <tr>
        <td><%= filo_esp_esp.parent_id %></td>
        <td><%= filo_esp_esp.child_id %></td>
        <td><%= link_to 'Show', filo_esp_esp %></td>
        <td><%= link_to 'Edit', edit_filo_esp_esp_path(filo_esp_esp) %></td>
        <td><%= link_to 'Destroy', filo_esp_esp, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Filo Esp Esp', new_filo_esp_esp_path %>
