<%= form_with(model: filo_esp_esp, local: true) do |form| %>
  <% if filo_esp_esp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(filo_esp_esp.errors.count, "error") %> prohibited this filo_esp_esp from being saved:</h2>

      <ul>
      <% filo_esp_esp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :parent_id %>
    <%= form.number_field :parent_id %>
  </div>

  <div class="field">
    <%= form.label :child_id %>
    <%= form.number_field :child_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
