<%= form_with(model: d_archivo) do |form| %>
  <% if d_archivo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(d_archivo.errors.count, "error") %> prohibited this d_archivo from being saved:</h2>

      <ul>
        <% d_archivo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :archivo, style: "display: block" %>
    <%= form.text_field :archivo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
