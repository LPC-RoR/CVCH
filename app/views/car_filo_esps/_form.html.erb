<%= form_with(model: car_filo_esp, local: true) do |form| %>
  <% if car_filo_esp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car_filo_esp.errors.count, "error") %> prohibited this car_filo_esp from being saved:</h2>

      <ul>
      <% car_filo_esp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :carpeta_id %>
    <%= form.number_field :carpeta_id %>
  </div>

  <div class="field">
    <%= form.label :filo_especie %>
    <%= form.number_field :filo_especie %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
