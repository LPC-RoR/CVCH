
<%= render partial: '0p/general/titulo', locals: {c: controller_name, a: nil, titulo: 'Colecciones', size: '5'} %>
<div class='row'>
	<div class='col-md-3'>
		<% if params[:search].blank? %>
		<%= render partial: '0p/list/selector', locals: {titulo: 'Áreas', list: @list_selector, sel: @sel, options: @options} %>
		<% end %>
	</div>
	<div class='col-md-9'>
		<%= render partial: '0p/search/form', locals: {path: vistas_path} %>
		<%= paginate @coleccion['publicaciones'], outer_window: 3 %>
		<small><small><%= page_entries_info(@coleccion['publicaciones']) %></small></small>
		<%= render partial: '0p/tabla/solo_tabla', locals: {c: 'publicaciones', a: 'index'} %>
		<%= paginate @coleccion['publicaciones'], outer_window: 3 %>
	</div>
</div>
