<%= form_with(model: filo_interaccion, local: true) do |form| %>
  <% if filo_interaccion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(filo_interaccion.errors.count, "error") %> prohibited this filo_interaccion from being saved:</h2>

      <ul>
      <% filo_interaccion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :filo_def_interaccion_id %>
    <%= form.number_field :filo_def_interaccion_id %>
  </div>

  <div class="field">
    <%= form.label :publicacion_id %>
    <%= form.number_field :publicacion_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
