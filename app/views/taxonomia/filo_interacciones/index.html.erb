<p id="notice"><%= notice %></p>

<h1>Filo Interacciones</h1>

<table>
  <thead>
    <tr>
      <th>Filo def interaccion</th>
      <th>Publicacion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @filo_interacciones.each do |filo_interaccion| %>
      <tr>
        <td><%= filo_interaccion.filo_def_interaccion_id %></td>
        <td><%= filo_interaccion.publicacion_id %></td>
        <td><%= link_to 'Show', filo_interaccion %></td>
        <td><%= link_to 'Edit', edit_filo_interaccion_path(filo_interaccion) %></td>
        <td><%= link_to 'Destroy', filo_interaccion, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Filo Interaccion', new_filo_interaccion_path %>
