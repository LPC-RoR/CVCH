<!-- como  es un formulario que se  puede utilizar para especies, pero para el registro de fuente también se llevó a un partial -->
<%= render partial: 'aplicacion/publicos/partials/dtl_esp_fte', locals: {form: form, destino: 'especie'} %>

<%= render partial: '0p/form_fields/check_box', locals: {form: form, token: :revisar, label: 'Revisar!'} %></br>

<%= render partial: '0p/form_fields/hidden', locals: { form: form, token: :filo_elemento_id } %>