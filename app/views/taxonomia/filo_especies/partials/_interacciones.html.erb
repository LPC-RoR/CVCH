<!-- VERIFICADO -->
<small class="text-info">
	<%= render partial: '0p/general/gly', locals: {gly: 'flower1'} %>
	Interacciones
</small></br>
<!-- LISTA DE INTERACCIONES REGISTRADAS PARA LA ESPECIE -->
<% @coleccion['filo_def_interacciones'].each do |filo_def_interaccion| %>
	<%= filo_def_interaccion.filo_def_interaccion %>
<% end %>

<% coleccion.each do |interaccion| %>
	<!-- Se puso el unless para resolver que se metían interacciones que no correspondían-->
	<%= render partial: 'taxonomia/filo_interacciones/list/lobjeto', locals: {objeto: interaccion} if interaccion.especies_ids.include?(objeto.id) %>
<% end %>

<%# objeto.filo_roles.order(:filo_rol).each do |filo_rol| %>
	<%#= render partial: 'taxonomia/filo_roles/list/lobjeto', locals: {objeto: filo_rol} %>
<%# end %>