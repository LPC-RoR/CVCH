<h5>
	<i class="bi bi-diagram-3 text-<%= params_indice.blank? ? 'info' : (objeto.revisar ? 'danger' : (objeto.mma_ok ? 'info' : 'dark')) %>"></i>
	<span class='text-info'>
		Taxonomía
		<i class="bi bi-dot"></i> 
		<%= params_indice.blank? ? 'Base de la estructura' : "[#{objeto.filo_orden.filo_orden unless objeto.filo_orden.blank?}] #{objeto.filo_elemento}" %>
	</span>
</h5>