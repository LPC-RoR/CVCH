<% unless base.blank? %>
	<table class='table-borderless'>
		<tbody>
			<tr>
				<td><small><b><i class="bi bi-diagram-2"></i><%= " [#{base.filo_orden.filo_orden unless base.filo_orden.blank?}] - #{base.filo_elemento} (#{base.n_especies})  " %><i class="bi bi-arrow-bar-right"></i></b></small></td>
				<% if controller_name == 'especies' %>
					<td><%= render partial: '0p/tabla/btns', locals: {a: 'index', objeto: base} %></td>
				<% end %>
			</tr>
			<%= render partial: 'taxonomia/filo_elementos/partials/show_nivel', locals: {elemento: base} unless base.children.empty? %>
			<%= render partial: 'taxonomia/filo_elementos/partials/show_especies', locals: {elemento: base} unless base.filo_especies.empty? %>
		</tbody>
	</table>
<% end %>