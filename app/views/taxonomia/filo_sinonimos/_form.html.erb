<%= form_with(model: filo_sinonimo, local: true) do |form| %>
  <% if filo_sinonimo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(filo_sinonimo.errors.count, "error") %> prohibited this filo_sinonimo from being saved:</h2>

      <ul>
      <% filo_sinonimo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :filo_especie_id %>
    <%= form.number_field :filo_especie_id %>
  </div>

  <div class="field">
    <%= form.label :filo_sinonimo %>
    <%= form.text_field :filo_sinonimo %>
  </div>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.text_field :tipo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
