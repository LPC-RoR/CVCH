<%= form_with(model: filo_esp_sino, local: true) do |form| %>
  <% if filo_esp_sino.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(filo_esp_sino.errors.count, "error") %> prohibited this filo_esp_sino from being saved:</h2>

      <ul>
      <% filo_esp_sino.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :filo_especie_id %>
    <%= form.number_field :filo_especie_id %>
  </div>

  <div class="field">
    <%= form.label :filo_sinonimo_id %>
    <%= form.number_field :filo_sinonimo_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
