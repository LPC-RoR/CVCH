<% unless @coleccion['observaciones'].empty? %>
	<%= render partial: '0p/general/titulo', locals: {c: 'observaciones', a: nil, titulo: 'Observaciones', size: '5'} %>
	<table class="table table-striped table-sm  thead-dark">
		<tbody>
			<% @coleccion['observaciones'].each do |obs| %>
				<tr><td><%= "| #{obs.observacion} |" %></td><td><%= render partial: '0p/tabla/btns', locals: {a: 'index', objeto: obs} %></td></tr>
				<tr><td><small><%= sanitize obs.d_detalle %></small></td></tr>
			<% end %>
		</tbody>
	</table>
<% end %>
<%= render partial: '0p/general/titulo', locals: {c: 'observaciones', a: nil, titulo: '+ Observacion', size: '5'} %>
<%= form_for :observacion_base, url: "/observaciones/nuevo?class_name=#{@objeto.class.name}&objeto_id=#{@objeto.id}" do |form| %>
	<div class='form-inline'>
		<%#= form.text_field(:orden, size: 5, placeholder: 'orden', class: 'form-control' ) %>
		<small><%= form.label :observacion %></small></br>
		<%= form.text_field(:observacion, size: 67, class: 'form-control' ) %>
	</div>
		<small><%= form.label :detalle %></small></br>
  	<%= form.text_area :detalle, size: '68x3' %>
	<%= form.submit '+ Observación'  %>
<% end  %>
