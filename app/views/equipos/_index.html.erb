<% coleccion.each do |equipo| %>
	<small>
	<%= link_to equipo.equipo, equipo %></br>
	<%= equipo.sha1 %></br>
	</small>
	<%= render partial: '0p/tabla/btns', locals: {a: 'index', objeto: equipo} if botones %>
	<hr>
<% end %>