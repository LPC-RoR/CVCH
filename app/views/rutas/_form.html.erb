<%= form_with(model: ruta, local: true) do |form| %>
  <% if ruta.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ruta.errors.count, "error") %> prohibited this ruta from being saved:</h2>

      <ul>
      <% ruta.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :instancia_id %>
    <%= form.number_field :instancia_id %>
  </div>

  <div class="field">
    <%= form.label :publicacion_id %>
    <%= form.number_field :publicacion_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
