<% elemento.filo_especies.each do |especie| %>
	<tr>
		<td><small><%= "#{("- " * 20)}[Especie] - #{especie.filo_especie}" %></small></td>
		<td><%= render partial: '0p/tabla/btns', locals: {a: 'index', objeto: especie} %></td>
	</tr>
	<% unless especie.children.empty? %>
		<%#= render partial: 'filo_elementos/partials/show_especies', locals: {elemento: especie} %>
	<% end %>
<% end %>
