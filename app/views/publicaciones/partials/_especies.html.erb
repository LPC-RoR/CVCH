<%#= render partial: '0p/general/icon_text', locals: {text: 'Gestión de Especies', gly: 'check-all', clr: 'info'} %>
</br>
<% if usuario_signed_in? %>
	<%= form_for :especie_base, url: "/especies/asigna?publicacion_id=#{@objeto.id}" do |form| %>
		<div class='form-inline'>
			<%= form.text_field(:especie, size: 35, class: 'form-control' ) %>
			<button type="submit" class="btn btn-info">
			  <i class="bi bi-plus-circle icon-white"></i>
			  <i class="bi bi-bug icon-white"></i>
			</button>
		</div>
	<% end  %>
<% end  %>

<%= render partial: '0p/list/action_list', locals: {nombre: 'Especies Etiquetadas', gly: 'card-checklist', clr: 'info', coleccion: @coleccion['especies'], field: 'especie', link: '/desasignar', gly_link: 'x-circle', base: true, action: usuario_signed_in?} %>
