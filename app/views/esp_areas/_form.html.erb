<%= form_with(model: esp_area, local: true) do |form| %>
  <% if esp_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(esp_area.errors.count, "error") %> prohibited this esp_area from being saved:</h2>

      <ul>
      <% esp_area.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :especie_id %>
    <%= form.number_field :especie_id %>
  </div>

  <div class="field">
    <%= form.label :area_id %>
    <%= form.number_field :area_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
