<small>

  <p>
    <% if admin? %>
    <a data-toggle="collapse" href="#collapseEnlaces" role="button" aria-expanded="false" aria-controls="collapseEnlaces">
      <i class="bi bi-link-45deg"></i>
    </a>
    <% else %>
      <i class="bi bi-link-45deg"></i>
    <% end %>

  <% if objeto.enlaces.empty? %>
    <span class='text-muted'>Sin enlaces ingresados</span>
  <% else %>
      <% objeto.enlaces.each do |enlace| %>
        <%= link_to enlace.descripcion, enlace.enlace, target: '_blank' %>
      <% end %>
  <% end %>

  </p>
  <div class="collapse" id="collapseEnlaces">
    <div class="card card-body">
    	<%= render partial: 'aplicacion/publicos/partials/form_nuevo_enlace', locals: {objeto: objeto} if admin? %>
    </div>
  </div>

</small>