<h5>
	<i class="bi bi-diagram-3 text-<%= params[:indice].blank? ? 'info' : (objeto.revisar ? 'danger' : (objeto.mma_ok ? 'info' : 'dark')) %>"></i>
	<span class='text-info'>
		Taxonomía | 
		<%= params[:indice].blank? ? 'Base' : "[#{objeto.filo_orden.filo_orden unless objeto.filo_orden.blank?}] #{objeto.filo_elemento}" %>
	</span>
</h5>
