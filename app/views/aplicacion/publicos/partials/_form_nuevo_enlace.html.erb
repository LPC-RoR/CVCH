<!-- FORMULARIO PARA NUEVO ELEMENTO HIJO -->
<small><b class='text-info'>Nuevo enlace</b></small>
<%= form_for :nuevo_enlace, url: "/filo_especies/#{objeto.id}/nuevo_enlace" do |form| %>

	<%= render partial: '0p/form_fields/label', locals: { form: form, label: 'Descripción', v: true } %>
	<%= render partial: '0p/form_fields/text', locals: { form: form, token: :descripcion } %>
	<%= render partial: '0p/form_fields/label', locals: { form: form, label: 'Enlace', v: true } %>
	<%= render partial: '0p/form_fields/text', locals: { form: form, token: :enlace } %>


	</br>
	<button type="submit" class="btn btn-info">
	  <i class="bi bi-plus-circle icon-white"></i>
	  <i class="bi bi-link"></i>
	</button>
<% end  %>
