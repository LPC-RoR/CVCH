<div class="tax-box">
	<div class='text-info'><small>
		<%= render partial: '0p/general/gly', locals: {gly: 'tag'} %> 
		Etiqueta de la publicación</small>
	</div>
	<small><small>
	<% objeto.especies.each do |tag| %>
		<div class="row">
			<div class="col-9">
				<div style="padding-left: 1em;">
					<%= render partial: '0p/general/gly', locals: {gly: 'dot'} %>
					<% if tag.especie == objeto.filo_especie %>
						<i class="bi bi-check-all"></i>
					<% end %>
					<%= link_to tag.especie, tag %>
				</div>
			</div>
			<div class="col-3" align="right">
				<%= render partial: '0p/general/gly', locals: {gly: 'files'} %> 
				<%= tag.publicaciones.count %>
			</div>
		</div>
	<% end %>		
	</small></small>
</div>