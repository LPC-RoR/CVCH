<%= render partial: 'aplicacion/publicos/partials/titulo_especie', locals: {objeto: @objeto} %>

Coleccion principal

<%= render partial: '0p/tabla/tabla', locals: {c: 'principal-publicaciones'} %>

Sinonimia

<% @etiquetas.each do |tag| %>
	Sinónimo : <%= tag.especie %>
	<%= render partial: '0p/tabla/tabla', locals: {c: "#{tag.especie.split(' '),join('#')}-publicaciones"} %>
<% end %>