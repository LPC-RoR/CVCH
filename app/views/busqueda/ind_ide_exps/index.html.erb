<p id="notice"><%= notice %></p>

<h1>Ind Ide Exps</h1>

<table>
  <thead>
    <tr>
      <th>Ind idea</th>
      <th>Ind expresion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ind_ide_exps.each do |ind_ide_exp| %>
      <tr>
        <td><%= ind_ide_exp.ind_idea_id %></td>
        <td><%= ind_ide_exp.ind_expresion_id %></td>
        <td><%= link_to 'Show', ind_ide_exp %></td>
        <td><%= link_to 'Edit', edit_ind_ide_exp_path(ind_ide_exp) %></td>
        <td><%= link_to 'Destroy', ind_ide_exp, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ind Ide Exp', new_ind_ide_exp_path %>
