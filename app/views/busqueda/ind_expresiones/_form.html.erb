<%= form_with(model: ind_expresion, local: true) do |form| %>
  <% if ind_expresion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ind_expresion.errors.count, "error") %> prohibited this ind_expresion from being saved:</h2>

      <ul>
      <% ind_expresion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ind_expresion %>
    <%= form.text_field :ind_expresion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
